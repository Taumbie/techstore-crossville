// ==========================
// Shared Types
// Centralized TypeScript types used by product-related components
// ==========================
export type Product = {
  id: number;
  title: string;
  price: number;
  description: string;
  category: string;
  image: string;
};
